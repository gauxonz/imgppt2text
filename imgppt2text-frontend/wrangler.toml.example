# Cloudflare Pages 配置示例
# 复制此文件为 wrangler.toml 并修改

name = "imgppt2txt"
compatibility_date = "2024-01-01"
pages_build_output_dir = "."

# 环境变量（唯一需要配置的是后端 API 地址）
[vars]
API_URL = "https://你的-workers-域名.workers.dev/"

# 部署后可在 Cloudflare Dashboard -> Pages -> 自定义域 中绑定自定义域名
